name: stillora_mobile
description: "Stillora mobile app for local image-to-video exports."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.3.0+22

environment:
  sdk: ^3.10.7

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  # Ships the Material/Cupertino/Widgets localizations for the app's supported
  # locales, and the RTL directionality Arabic needs.
  flutter_localizations:
    sdk: flutter

  cupertino_icons: ^1.0.8
  crypto: ^3.0.6
  image: ^4.3.0
  # Store Screenshots exports every rendered size as one .zip. Already present
  # transitively (via `printing`); declared directly because we import it.
  archive: ^4.0.7
  dio: ^5.9.0
  equatable: ^2.0.7
  file_picker: ^10.3.7
  font_awesome_flutter: ^11.0.0
  flutter_riverpod: ^3.0.3
  flutter_svg: ^2.0.10
  flutter_secure_storage: ^10.0.0
  go_router: ^17.0.0
  google_sign_in: ^7.2.0
  image_picker: ^1.2.1
  path_provider: ^2.1.5
  share_plus: ^12.0.1
  shared_preferences: ^2.5.3
  stillora_video_engine:
    path: packages/stillora_video_engine
  video_player: ^2.10.1
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  url_launcher: ^6.3.1
  in_app_review: ^2.0.12
  sign_in_with_apple: ^7.0.1
  record: ^6.0.0
  audioplayers: ^6.1.0
  gal: ^2.3.0
  permission_handler: ^12.0.0
  # Images/PDF → PDF section. `pdf` is pure Dart (writes the output document);
  # `printing` is only used for Printing.raster, which rasterizes the pages of
  # an imported PDF so they can be re-laid-out alongside the images.
  pdf: ^3.13.0
  printing: ^5.15.0
  in_app_purchase: ^3.3.0
  # Direct so the billing service can take `InAppPurchasePlatform` as its seam
  # and be unit-tested without a store. Same package `in_app_purchase` pins.
  in_app_purchase_platform_interface: ^1.4.1

# path_provider_foundation 2.5.0+ migrated to the `objective_c` package, which
# uses Flutter's native-assets/code-assets build hooks. On Flutter 3.38.x stable
# that path fails with "native_assets required define SdkRoot", and the
# objective_c.framework never gets embedded. Pin to the last classic-plugin
# release until native assets stabilize.
dependency_overrides:
  path_provider_foundation: 2.4.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  uses-material-design: true

  # stillora_video_engine and sign_in_with_apple have no Swift Package Manager
  # support, and Xcode fails to resolve SPM dependencies for the macOS target
  # because of it — which breaks `flutter build/drive -d macos` outright. Pin
  # the project to CocoaPods until both plugins adopt SPM.
  config:
    enable-swift-package-manager: false

  assets:
    - assets/logo/

  fonts:
    # Cairo (SIL OFL 1.1, see assets/fonts/OFL.txt) is used for Arabic, whose
    # script the Latin-first UI font renders poorly. These are the Google Fonts
    # `arabic` subset instances, which also carry Latin glyphs, so mixed
    # Arabic/Latin strings ("Stillora", "10 ثانية") stay in one typeface.
    - family: Cairo
      fonts:
        - asset: assets/fonts/Cairo-Regular.ttf
          weight: 400
        - asset: assets/fonts/Cairo-Medium.ttf
          weight: 500
        - asset: assets/fonts/Cairo-SemiBold.ttf
          weight: 600
        - asset: assets/fonts/Cairo-Bold.ttf
          weight: 700
        - asset: assets/fonts/Cairo-ExtraBold.ttf
          weight: 800
        - asset: assets/fonts/Cairo-Black.ttf
          weight: 900

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
