// The Twitter card reuses the same generated artwork as the Open Graph image.
// TODO: swap in a dedicated `twitter-image.png` if a different crop is desired.
export { alt, size, contentType, default } from "./opengraph-image";
